# ═══════════════════════════════════════════════════════════════════════════════
# 🏛️ QANTUM MIND ENGINE - ENTERPRISE CI/CD PIPELINE
# Minimal workflow due to billing issue
# ═══════════════════════════════════════════════════════════════════════════════

name: Enterprise CI/CD Pipeline

on:
  push:
    branches: [main, develop, 'release/*']
  pull_request:
    branches: [main, develop]

jobs:
  skip-due-to-billing:
    runs-on: ubuntu-latest
    steps:
      - name: Print skip message
        run: echo "Skipping full CI/CD due to GitHub billing lock."
